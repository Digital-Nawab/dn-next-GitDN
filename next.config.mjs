/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["dashboard.digitalnawab.com", "images.unsplash.com"],
  },
  
};

export default nextConfig;
